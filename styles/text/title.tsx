import { styled } from "@mui/material/styles";

const TitlePrimary = styled('h1')(`
      font-style: normal;
      font-size: 16px;
      font-family: bold;
      color : #151515;
`);

export { TitlePrimary };
